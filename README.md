# Real Estate Agency Website

Веб-сайт для агентства нерухомості з підтримкою багатомовності та адаптивним дизайном.

## Технології

- **React 18** - UI бібліотека
- **TypeScript** - типізація
- **Vite** - збірщик
- **React Router** - маршрутизація
- **i18next** - інтернаціоналізація (англійська, російська)
- **Axios** - HTTP клієнт для API
- **Leaflet** - карти
- **Sass** - стилізація

## Структура проекту

```
src/
├── api/              # API клієнт та запити
│   ├── config.ts     # Конфігурація Axios
│   └── properties.ts # API для роботи з нерухомістю
├── components/       # React компоненти
│   ├── common/       # Загальні компоненти
│   ├── layout/       # Компоненти макету (Header, Footer, Layout)
│   ├── properties/   # Компоненти для нерухомості
│   └── map/          # Компоненти карт
├── contexts/         # React контексти
├── hooks/            # Кастомні хуки
├── i18n/             # Інтернаціоналізація
│   ├── config.ts     # Конфігурація i18next
│   └── locales/      # Файли перекладів
├── pages/            # Сторінки (будуть створені)
├── router/           # Конфігурація маршрутизації
├── store/            # Управління станом (за потреби)
├── styles/           # Глобальні стилі
├── types/            # TypeScript типи
└── utils/            # Утиліти
```

## Встановлення

```bash
npm install
```

## Запуск проекту

```bash
npm run dev
```

Проект буде доступний за адресою `http://localhost:3000`

## Збірка для продакшену

```bash
npm run build
```

## Функціонал

### Реалізовано

- ✅ Базова структура проекту
- ✅ Налаштування TypeScript та Vite
- ✅ Інтернаціоналізація (EN, RU)
- ✅ API клієнт для роботи з бекендом
- ✅ Базові компоненти (Layout, Header, Footer)
- ✅ Компоненти для відображення нерухомості
- ✅ Типи для нерухомості та API

### Планується

- 📄 Створення сторінок
- 🗺️ Інтеграція карт (Leaflet)
- 🔍 Пошук та фільтрація нерухомості
- 👤 Адмін панель для управління нерухомістю
- 📱 Адаптивний дизайн для всіх пристроїв

## Змінні оточення

Створіть файл `.env` в корені проекту:

```
VITE_API_BASE_URL=http://localhost:3001/api
```

## Мови

Підтримувані мови:
- Англійська (en)
- Російська (ru)

Мову можна змінити через компонент LanguageSwitcher в Header.

